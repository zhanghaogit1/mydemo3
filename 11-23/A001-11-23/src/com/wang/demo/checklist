interface 接口，定义接口（极端抽象的类）
implements
怎么写？格式
怎么用？作用

(1)接口格式：
	属性：默认public, 默认static，默认final
	构造：不能定义
	方法：默认public, 默认abstract,
	
	接口可以声明变量，不能new对象。
	
(2)作用：定义规则。子类强制实现规则。
(3)案例作用

(4)实现多接口
	是否可以实现多接口？可以
	implements USB f1(), HDMI f2(), xxx f2()
	重写了每个接口中定义的抽象方法
	f1()
	f2()
	f3()

(5)接口多重继承
	接口多重继承？可以
	接口实现接口？不可以

多态：
父类的引用可以引用子类的对象。
父引用子类。
继承和重写。
统管整个类族。









